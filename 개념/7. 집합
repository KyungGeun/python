# 집합: 중복을 허용하지 않고 순서가 없는 자료형

# 집합 만드는 법
a = 1, 2, 1, 3
st = set(a)
print(st) # 출력: {1, 2, 3}
a = list(a)
st = set(a)
print(st) # 출력: {1, 2, 3}
st = set('hello')
print(st) # 출력: {'o', 'e', 'l', 'h'}
          # 출력: {'l', 'h', 'e', 'o'} 중복을 허용하지 않으므로 'l'이 한 개가 됐고, 순서가 없으므로 출력할 때 마다 순서가 랜덤이다.

# 연산이 가능하다.
s1 = set([1, 2, 3, 4])
s2 = set([3, 4, 5])
s3 = set([6, 7, 8])

# 합집합 |
print(s1 | s2)  # 출력: {1, 2, 3, 4, 5}
print(s2 | s3)  # 출력: {3, 4, 5, 6, 7, 8}

# 교집합 &
print(s1 & s2)  # 출력: {3, 4}
print(s2 & s3)  # 출력: set()

# 차집합 -
print(s1 - s2)  # 출력: {1, 2}
print(s2 - s3)  # 출력: {3, 4, 5}

# 등호(=)와 같이 쓸 경우 연산과 대입을 동시에 하게 된다.
s1 |= s2
print(s1)  # 출력: {1, 2, 3, 4, 5}
s1 &= s2
print(s1)  # 출력:{3, 4, 5}
s1 -= s2
print(s1)  # 출력: set()

# 대칭 차집합 ^(합집합 - 교집합)
print(s1 ^ s2)  # 출력: {3, 4, 5}
print(s2 ^ s3)  # 출력: {3, 4, 5, 6, 7, 8}
